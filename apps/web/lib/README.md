# Shared web infrastructure

Use this directory only for cross-feature infrastructure such as authenticated server clients, permission helpers, dates, and application services. Product concepts belong in their named feature or in `packages/domain`.

