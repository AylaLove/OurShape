# Visual assets

The first interface uses Lucide icons and code-native household geometry. Add custom bitmap assets only when they communicate something the icon system cannot. Every asset needs a clear owner, accessible alternative, and usage record.

