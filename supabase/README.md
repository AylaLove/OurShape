# Supabase boundary

This directory will contain reviewed database migrations, local Supabase configuration, and database tests.

No remote project is connected in the foundation checkpoint. The identity batch must establish a dedicated development project rather than reusing The Hearth or Zamkee credentials.

Real credentials belong in `.env.local`, never in this directory.

